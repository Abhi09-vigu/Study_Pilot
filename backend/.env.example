# Study-Pilot Backend Environment Variables (example)

# MongoDB connection string
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority

# JWT secret for authentication (change in production)
JWT_SECRET=change_me

# Email (optional for reminders)
EMAIL_USER=
EMAIL_PASS=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# OpenRouter (optional for AI planning)
OPENROUTER_API_KEY=

# Port for backend server
PORT=4001

# Frontend URLs (for CORS / links) — fill these with your actual frontend domains
FRONTEND_URL=http://localhost:5173
# FRONTEND_URL=https://YOUR_NETLIFY_SITE.netlify.app
